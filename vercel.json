{
  "version": 2,
  "builds": [
    {
      "src": "apps/staking-web/package.json",
      "use": "@vercel/next"
    }
  ],
  "installCommand": "pnpm install --frozen-lockfile",
  "buildCommand": "pnpm --filter '@monad-staking/config' build && pnpm --filter '@monad-staking/sdk' build"
}
